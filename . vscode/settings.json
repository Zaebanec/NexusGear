{
  // --- НАЧАЛО БЛОКА ДЛЯ ФОРМАТИРОВАНИЯ ---
  // Эти настройки должны быть в вашем файле.
  // Если у вас уже есть секция "[python]", добавьте эти строки в нее.

  "[python]": {
    // 1. Устанавливаем Black как форматер по умолчанию для Python.
    // Он будет применяться последним.
    "editor.defaultFormatter": "ms-python.black-formatter",

    // 2. Включаем форматирование при каждом сохранении файла.
    "editor.formatOnSave": true,

    // 3. Запускаем "быстрые исправления" от Ruff ПЕРЕД форматированием Black.
    // Ruff уберет неиспользуемые импорты, отсортирует их и исправит
    // сотни других мелочей за миллисекунды.
    "editor.codeActionsOnSave": {
      "source.fixAll": "explicit",
      "source.organizeImports": "explicit"
    }
  }

  // --- КОНЕЦ БЛОКА ---

  // ... (здесь могут быть другие ваши настройки для проекта) ...
}